; Permite subir PDFs/dossiers pesados desde admin.html (api/upload-doc.php, límite de app = 50MB).
; PHP-FPM cachea .user.ini unos minutos (user_ini.cache_ttl) tras cada deploy.
upload_max_filesize = 60M
post_max_size = 60M
