# Secretos y archivos generados — nunca en git
private/mail.php
vendor/

# Artefactos de prueba locales
_demo_ppjb.html
templates/_demo_*.html
_originales/
.playwright-mcp/
__pycache__/
# harness de verificación (copia de app.html con Supabase stubeado) y su salida
_test_app.html
_preview_doc.html
# rediseño v2 del panel — SOLO local, que lo vea el usuario antes de decidir; nunca desplegar
app.v2.html
_preview_v2.html
_test_v2.html
_repro_num.html

# Vía .docx abandonada (pivote a plantilla HTML) — 11MB de binarios obsoletos
private/plantillas/

# Material fuente del cliente y exports de prueba — NO versionar ni desplegar
# (contratos fuente = confidenciales; se servían en público desde /contracts/)
/*.pdf
/Firma.png
Logo Lawang-*

# Informes INTERNOS del estudio sobre los contratos (revision legal, coherencia,
# resumen de cambios). El repo es publico y esta carpeta se sirve en /contracts/
# → nunca versionar: seria analisis de riesgo legal del cliente leible por cualquiera.
informe_*.html
resumen_cambios_*.html
revision_legal_*.html
# credencial local del CLI linkado — nunca al repo
supabase/.temp/
